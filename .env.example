# API URL (varsayılan: https://api.optimai.network)
API_URL=https://api.optimai.network

# WebSocket URL (varsayılan: wss://ws.optimai.network)
WS_URL=wss://ws.optimai.network

# Ping aralığı (varsayılan: her 5 dakikada bir)
PING_CRON=*/5 * * * *

# Daily claim zamanı (varsayılan: rastgele)
# DAILY_CLAIM_CRON=30 14 * * *

# Weekly claim zamanı (varsayılan: rastgele)
# WEEKLY_CLAIM_CRON=0 15 * * 0

# Tek hesap için refresh token (accounts.json kullanmıyorsanız)
REFRESH_TOKEN=your_refresh_token_here